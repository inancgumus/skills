slack/scripts/slack.py owns all Slack-specifics: "DOM selectors, ab/ab_eval calls, CDP interaction, navigation, parsing". Scripts (e.g., get.py) own workflow logic: loops, pagination, filtering, output formatting. Scripts compose slack.py. When adding a new Slack interaction, add a primitive to slack.py. When adding a new workflow, add a script that calls existing primitives. If a script needs Slack operation that doesn't exist yet, add the primitive to slack.py first. Test after changes by taking screenshots at each step and never assume the UI state. Update ~/.claude/skills after changes.
